directive @auth on FIELD_DEFINITION
directive @admin on FIELD_DEFINITION

type Query {
    _empty: String
}

type Mutation {
    _empty: String
}

enum VerificationType {
    EMAIL
    SMS
}

input PageInput {
    page: Int! = 1
    size: Int! = 10
}